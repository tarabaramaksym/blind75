const str = "cabacxxdabadxx";

var longestPalindrome = function (s) {

  let left = 0;
  let right = s.length - 1;
  for (let i = 0; i < s.length; i++) {
    console.log(s[i]);
  }
};

console.log(longestPalindrome(str));